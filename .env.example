# Optional: URL of a serverless/backend endpoint that accepts POST { name, email, subject, message }
# and delivers it to your inbox. If unset, the contact form falls back to a mailto: link.
VITE_CONTACT_ENDPOINT=
